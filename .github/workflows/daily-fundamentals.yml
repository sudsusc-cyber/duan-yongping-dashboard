name: Daily Fundamentals Refresh

# Refresh 5Y price percentile snapshot from Yahoo Finance.
# Runs daily after US close (~22:00 UTC = 06:00 Beijing next day).
# No SEC secret needed — Yahoo's chart API is unauth + UA-only.

on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      - name: Fetch 5Y price percentile snapshot
        run: npx tsx scripts/fetch-fundamentals.ts

      - name: Commit if data changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/fundamentals/
          if git diff --cached --quiet; then
            echo "No fundamentals delta — skipping commit."
          else
            git commit -m "chore(data): daily 5Y price percentile refresh"
            git push
          fi
